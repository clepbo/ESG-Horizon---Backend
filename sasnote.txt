// i want us to do a major refactoring to how we are handling our assessment in my ESG app.
// first i will start by sharing the assessment model
id               Int               @id @default(autoincrement())
subsidiary       String
status           AssessmentStatus  @default(in_progress)
createdAt        DateTime          @default(now())
updatedAt        DateTime          @updatedAt
assessmentData   Json
companyId        Int
created_by       Int?
endMonth         String
endYear          String
startMonth       String
startYear        String
updated_by       Int?
rejection_reason String?
reviewedAt       DateTime?
reviewed_by      Int?
company          Company           @relation(fields: [companyId], references: [id])
creator          User?             @relation("CreatedAssessment", fields: [created_by], references: [id])
reviewer         User?             @relation("ReviewedAssessment", fields: [reviewed_by], references: [id])
updater          User?             @relation("UpdatedAssessment", fields: [updated_by], references: [id])
DisclosureTopic  DisclosureTopic[]
report           Report?
  
  
// this is my expectation for how we should structure our assessment records:
id,
company: {name},
subsidiary: {name},
status,
createdAt (normal default),
updatedAt (normal default),
created_by: {name},
startMonth,
startYear,
endMonth,
endYear,
updated_by: {name},
rejection_reason,
reviewed_by: {name},
reviewedAt (normal default),
assessmentData: {
    topEmissionSources: {
        // top 5 emission sources, this will be sent from the frontend
    },
    dataCount: {
        expected: number, // total number of metrics (fields) across the entire possible assessment (i.e. across all pillars)
        count: number,  // count of all metrics (i.e. fields) that a user save/submitted. we need to decide if we'll count on the frontend or in the backend service
    },
    overallProgress (percentage), // to be calculated from percentages of each pillars/scopes (i.e. subtopics)
    totalEmission: number, // number (total calculation for each subtopic)
    environment: { // this is the pillar we are handling for this v1
        // environment pillar has 3 topics, greenhouse gas emission, water management, air quality and biodiversity,
        // each topic has subtopics, which can be called anything, but for ghg it is scope1, scope2 and scope3
        dataCount: {
            expected, // total expected fields to fill for all forms for this pillar
            count, // for this pillar
        },
        progress (percentage), // for this pillar, comes from the frontend
        totalEmission: // number (total calculation for each subtopic)
        ghg: {
            dataCount: {
                expectedCount, // for ghg
                count, // what was filled under ghg
            },
            progress (percentage), // total percent combination for ghg subtopics/scopes
            totalEmission: // for ghg
            scope1: {
                dataCount: {
                    expectedCount: // for scope 1 groups of forms
                    count: // what was actually filled and saved/submitted
                },
                progress: // percent for this scope 1, which will be percentages for all the form groups under scope 1 - i call the form groups because they are made up of form steps in different files, and each form step can be saved, but the submit is at the last form of the group, but note that submission is done in each form group's last form, but that doesn't mean the asessment taking is over as the user can navigate after submitting to submit more groups and it all adds into the assessmentData,
                percent contribution to overall progress, // i.e. how many percent is the scope1 part of the overallProgress of the assessmentData,
                percent contribution to its pillar (i.e. environment),
                percent contribution to its topic (i.e. ghg),
                totalEmission: number, // for scope1 i.e. total of each groups
                stationarySources: { // now this is one forms group
                    dataCount: {
                        expectedCount: // for stationary sources 3 forms
                        count: // what was actually filled and saved/submitted
                    },
                    progress: // percent for stationary sources forms, also comes from the frontned
                    electricityHeat: { // now this is the actual form, that has 2 compulsory fields to fill
                        // field1 - when I share the frontend you will get better idea on the metric fields are
                        // field2
                    },
                    industrialProcess: {

                    },
                    oilGasOperations: {

                    }
                },
                mobileSources: { // another group

                },
                processEmissions: { // third group

                },
                fugitiveEmissions: { // and last group of scope one

                },
            },
            scope2: {

            },
            scope3: {
            },
        },
        airQuality: {
            // can be empty, we only handle ghg under this environment pillar in this v1
        },
        waterManagement: {
            // can be empty too
        },
        biodiversityImpact: {
            // can be empty too
        },
    },
    social: {
        // can be empty for now, we'll just return to indicate data is coming in next version
    }, 
    governance: {
        // can be empty for now too
    },
}

// now we could send in most of these from the frontend, or calculate some (or all we can) in the frontend, i will share the backend and frontend codes then we decide the way to go.
// also some additional requirements are - we shouldn't have more than one assessment for the same duration, i.e. the start and end month and years cannot be same with another assessment record, so let us handle that in the endpoint as well, if a user tries this, we tell them something along the lines of: "Assessment for this period already exists, continue it or delete and start a new one for same period"..
// note that in our frontend, even though this is how the backend expects that data.. but we submit in groups within the subtopics.. but more on this when we get to the frontend, 
// the first task now is to see what we need to touch/align so our backend handles this smoothly
// the good thing is our assessmentData is json, so it should be able to handle this, but we need to make sure we are passing our data in the specific structure we want
// another good thing is many of the dataCount and progress is calculation from the child upwards to each parent, if you get what I mean

// now i will share the backend dto, controller and services for this, we are using NestJs.



=====================
11. Progress towards target(in percentage) // kc
12. Baseline emission, current emission and target emission
13. Top 5 emission sources with their scope, total emission, name of the source.

========================


I am currently working on handling assessment and computation for it.

attached files are what we've done thus far for handling assessment submission and saving.

our assessment is huge and growing, hence why we are even storing the assessment data as json, to accomodate much more data that will come later.

we have mostly handled for ghg scope 1 and a bit of scope 2.

your task is to expand our calculator/computation logic and dtos to handle other pillars.

explanation of assessment data:

====================================

We have contracted you, a senior technical leader to expand and improve our assessment and report service. including targets.
What we want now is to expand the assessment service (and computation/calculations) and also the report endpoints.

1. the ESG assessment is made up of pillars, which are environment, social, and governance.
For this MVP, we are only working on environment pillar.

2. each pillar has topics under it,
2a. environment has ghg (greenhouse gas emissions), air quality, water management, biodiversity impact.

3. each topic might or might not have subtopics under them.
3a. for environment ghg, the subtopics: are scope1, scope2, and scope3.
3b. for environment air quality has no subtopic, so it jumps to metric.
3c. for environment water management the subtopics are: water and produced water management, hydraulic fracturing impacts.
3d. for environment biodiversity impact the subtopic is: environmental management.

4. each suptopic has metrics (which are basically the form groups i.e. groups of form steps from 1 to n)
4a ghg scope 1 and 2 metric forms has already been handled in this codebase already, as you can see (e.g. stationary sources, mobile sources, tc.. location and market based too). So we will skip and do the rest.
4b. for airQuality the metrics is: air pollutant emissions.
4c. for water and waste management the metrics forms groups are: (for the 2 subtopics (3c above), subtopic 1 has freshwater withdrawals & consumption, produced water managemet. WHile subtopic 2 has Chemical disclosure, water quality impacts)
4d. for biodiversityImpact the metrics forms groups are: (under the only subtopic, i.e. environmental management) - environmental management policies, hydrocarbon spills, reserves in sensitive areas.


5. each metric has forms steps (obviosuly, we mentioned this alread) consisting of some compulsory fields:
5a. (remember we have solved for ghg scope 1 and 2, so we will start wiht airQuality)
5b. for air quality's air pollutant emissions, the  fields for each steps are:
5b i. step1: { oxides of nitrogen: {volume: number, unit: string}, oxides of sulphur: {volume, unit}, volatic organic compounts: {v, u}, particulate matter: {v, u}} - thats all. then we submit here (and we can save too to come back and submit later).
5c. for water and waste manegemnt:
5c i. remember the first subtopic (i.e. water and produced water management) has freshwater withdrawals & consumption, produced water managemet.
5c ii. freshwater withdrawals & consumption step1 fields: {withdrawal from surface water: {v, u}, Withdrawal from Groundwater: {v, u}, Withdrawal from Municipal & Other Sources: {v, u}, Total Water Consumed: {v, u}, Volume Withdrawn from Water-Stressed Regions: {v, u}}. then save or next. 
step2 fields: {total produced water generated: {v, u}, volume discharged to surface: {v, u}, volune injected for Disposal: {v, u}, volume recycled/reused: {v, u}}. then save or next.
step3 field (radio): {does your company operate hydraulically fractured wells during the reporting period?: yes, no}. if a user clicks no, then submit (no next step), but if a user clicks yes, then next step is more fields
step4 (users reach this step if they choose yes): {number of wells with public disclosure of all chemicals: {v, u}, volume recycled/reused: {v, u}}. then submit.

5c iii. produced water managemet:
For this, step1: total produced water, volume discharged to surface, volume injected for disposal, volume recycled/reused (each expecting a volume and unit to be filled) then submit for now (there are other steps, but we will add them after this is done).


5c iv. remember the second subtopic (i.e. hydraulic fracturing impacts.) has Chemical disclosure, water quality impacts. this can submit empty objects for now.


5d.lets move to biodiversity impact's metrics under the only subtopic:
5d i. environment management policies step1: { is the environmental management system (ems) iso 14001 certified?: yes, no }, description of environmental management policies and practices: desc }. then next.
ii. step2: {number of spills, total volumne spilled, volumne recovered from environment, volumne in arctic, volume impacting sensitive shorelines} each expecting volume and unit. then next.
iii. step3: {total proved reserves, proved reserves in sensitive areas, total probable reserves, probable reserves in sesnsitive areas} each also volumen and unit. then submit.


Extra note:
- Each of the fields for users to fill (and image upload) are what we use to calculate percentage and count and expected count.
- We have already handled computation for environment ghg scope 1 and 2 (we will add scope 3 later) for now we want to now expand our assessment data to include and make sure we capture the other topics for environment.

======== END ASSESSMENT AND ENTER REPORT (WHICH WORKS WITH ASSESSMENT ANYWAY).

NOW FOR THE REPORTS::::
- endpoint to return all the list of reports for the user's company. this returns the report title, start and end durations, and status, and completed percentage of the assessments which each report has/is for.
- endpoint to return the report details (which are from the assessments submitted), here we need totals for each pillar (i.e. total emissions for environment, total emissions for social, etc.. - but since we are only handling environment, then all the other pillars will simply return zero). then nested within the environment, we want to see total emissions for scope1, 2 and 3.
we also want to see our target information (for targets we set, returning the baseline, current, and target). then also (and this is the tricky part), we now need to return emissions from each metric form group (note that when we submit any group, there is an emission calculated for the forms within that metric group i.e., the total emission from each field filled within each step of a particular metric group, to use our ghg scope1 as example, stationarySources is one group, and within it we have forms/steps of electricityHeat, industrialProcess, and oilGasOperations.. and we submit at the last step and it shows us the total emissions for stationarySources - which is emission from the 3 steps. Get it? now with many other metric groups, the form steps may not have names like stationarySources does, instead we use step1, step2,etc.. like we have defined above.)

Now do some magic.