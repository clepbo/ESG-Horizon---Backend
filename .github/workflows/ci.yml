name: NestJS CI/CD

on:
  push:
    branches:
      - main
      # Note: We only deploy to the server on 'main' push to keep things simple for the demo

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Prisma generate
        run: npx prisma generate --schema=./prisma/schema

      # - name: Run lint
        # run: npm run lint

      # - name: Run tests with coverage
        # run: npm run test:cov

      # - name: Generate coverage badge  # Optional: Keep if you use it for the README
      #   run: npm run make-badge

      - name: Build project
        run: npm run build

      # --- DEPLOYMENT STEPS START HERE ---
      
      - name: Transfer Files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          timeout: 60s
          command_timeout: 15m
          # We send: dist (code), package files, and prisma (for migrations)
          source: "dist,package.json,package-lock.json,prisma"
          target: "/var/www/esghorizon/backend"

      - name: Start via PM2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # 1. Force the shell to find Node, NPM, and PM2
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            
            cd /var/www/esghorizon/backend
            
            # 2. Production setup
            npm install --production
            npx prisma migrate deploy
            
            # 3. Restart the engine
            pm2 delete esg-backend || true
            pm2 start dist/main.js --name "esg-backend"
            pm2 save