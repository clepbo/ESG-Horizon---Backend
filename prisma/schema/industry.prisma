model Industry {
    id       Int    @id @default(autoincrement())
    sector   String
    industry String

    companies Company[]
    subsidiaries Subsidiary[]

    @@unique([sector, industry])
    @@map("industries")
}
