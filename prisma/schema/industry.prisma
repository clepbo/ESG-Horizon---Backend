model Industry {
  id           Int          @id @default(autoincrement())
  sector       String
  industry     String
  subsidiaries Subsidiary[]
  companies    Company[]

  @@unique([sector, industry])
  @@map("industries")
}
